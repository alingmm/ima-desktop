{
  "name": "ai-workstation",
  "version": "1.0.0",
  "description": "IMA AI 工作台 - 智能助手平台",
  "author": "IMA Team",
  "license": "MIT",
  "private": true,
  "main": "dist-electron/main.js",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "uuid": "^10.0.0"
  },
  "scripts": {
    "install:all": "pnpm install",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build:all": "pnpm install --prefer-offline && pnpm --filter client build && pnpm --filter server build",
    "start": "pnpm --filter server start",
    "electron:compile": "tsc -p tsconfig.electron.json",
    "electron:dev": "concurrently -k -n \"SERVER,CLIENT,ELECTRON\" -c \"blue,green,magenta\" \"pnpm --filter server dev\" \"pnpm --filter client dev\" \"wait-on http://localhost:5173 && tsc -p tsconfig.electron.json && electron .\"",
    "electron:build": "pnpm build:all && pnpm electron:compile && electron-builder -c electron-builder.json",
    "electron:build:win": "pnpm build:all && pnpm electron:compile && electron-builder -c electron-builder.json --win",
    "electron:build:mac": "pnpm build:all && pnpm electron:compile && electron-builder -c electron-builder.json --mac",
    "electron:build:linux": "pnpm build:all && pnpm electron:compile && electron-builder -c electron-builder.json --linux"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.0",
    "wait-on": "^8.0.0"
  }
}
